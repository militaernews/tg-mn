kurigram==2.2.22
# tgcrypto needs a C compiler to build (not present in the python:3.12-slim
# image these bots run in) - pyrogram falls back to pure-Python crypto fine,
# just somewhat slower. Same tradeoff tg-nn already made.
deepl==1.19.1
deep-translator==1.11.4
python-dotenv==1.0.1
regex==2024.9.11
pytest==8.3.3
psycopg-pool==3.2.3
psycopg-binary==3.2.3
requests==2.32.3